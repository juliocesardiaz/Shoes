<html>
<head>
	<title>Shoes</title>
</head>
<body>
	<h1>{{ brand.getName }}</h1>
	
	{% if stores is not empty %}
		<p>Here are the Stores for this Brand:</p>
		<ul>
			{% for store in stores %}
				<li>{{ store.getName }}</li>
			{% endfor %}
		</ul>
	{% endif %}
	
	<h4>Add a Store that is carrying this Brand</h4>
	
	<form action='/add_stores' method='post'>
		<input id="brand_id" name="brand_id" type="hidden" value="{{ brand.getId() }}">
		<label for="store_id">Select a store </label>
		<select name="store_id" id="store_id" type="text">
			{% for store in all_stores %}
				<option value="{{ store.getId }}"> {{ store.getName }} </option>
			{% endfor %}
		</select>
		<button type='submit'>Add store</button>
	</form>
	
	<p><a href='/brands/{{ brand.getId }}/edit'>Edit this Brand</a></p>
	
	<p><a href='/'>Home</a></p>
</body>
</html>